Send a message to
= @rec.name
= form_tag({controller: "messages", action: "create"}, method: "post") do
  = label_tag :subject
  = text_field_tag :subject
  = label :body, "Message text"
  = text_area_tag :body
  = hidden_field_tag(:user, @rec.id.to_s)
  = submit_tag 'Send message', class: "btn btn-primary"